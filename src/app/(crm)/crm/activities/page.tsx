import ActivityCenter from "@/modules/crm/views/ActivityCenter";

export const metadata = {
  title: "Interaction Hub | CRM MotorERP",
  description: "Schedule visits, calls, and follow-ups with customers.",
};

export default function ActivitiesPage() {
  return <ActivityCenter />;
}
