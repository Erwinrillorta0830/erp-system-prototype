import PRList from "@/modules/scm/views/PRList";

export const metadata = {
  title: "Purchase Requisitions | SCM MotorERP",
  description: "Track internal demand and procurement requests.",
};

export default function PRListPage() {
  return <PRList />;
}
